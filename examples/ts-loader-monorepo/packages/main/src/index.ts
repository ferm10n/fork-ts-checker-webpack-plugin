import { ProbablyANumber } from 'fork-ts-checker-webpack-plugin-ts-loader-monorepo-example-util/src';

// try changing the ProbablyANumber type in the util package
const num: ProbablyANumber = 'five';
console.log('Hello world', num);
